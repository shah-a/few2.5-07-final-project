// import useD3 from '../../hooks/useD3';

const index = ({ data }) => {
  console.log(data);

  return (
    <div className="index">
      <h1>Salaam :)</h1>
    </div>
  );
};

export default index;
